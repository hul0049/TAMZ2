package com.example.hul0049_tamz2_lode.Classes;

import android.graphics.Canvas;

import com.example.hul0049_tamz2_lode.Classes.Board;

public class canvas {
    protected void onDraw(Canvas canvas, Board board) {
   /*     Paint tbPaint = new Paint();
        tbPaint.setColor(Color.BLACK);
        tbPaint.setTextSize(200);
        if(win==false) {
            for (int i = 0; i < lx; i++) {
                for (int j = 0; j < ly; j++) {
                    canvas.drawBitmap(bmp[level[i * ly + j]], null,
                            new Rect(j * width, i * height, (j + 1) * width, (i + 1) * height), null);
                }
            }
        }
        else
        {
            canvas.drawText("WINNER!",100,700,tbPaint);
        }*/
    }
}
